# 叶育铨个人作品集

请不要直接双击 `index.html`。这是 React + Vite 项目，需要通过本地服务预览。

最简单的方式是双击 `启动预览.bat`，浏览器会自动打开 `http://localhost:5173/`。

也可以在本目录终端运行：

```powershell
pnpm dev
```

后续收到首屏视频后，将文件命名为 `hero.mp4` 并放到 `public/media/` 中即可。
